#include <stdio.h>
/**
 * main - A program that prints a line with puts function
 * Return: 0 (Success)
 */
int main(void)
{
	int myNum;
	scanf("%d", &myNum);
	if(myNum > 0)
	{
		printf("%d is positive\n", n );
	}
	else if (myNum == 0)
	{
		printf("%d is zero\n", n );
	}
	else
	{
		printf("%d is negative\n", n );
	}

}
